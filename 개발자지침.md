# AJW App Hub 개발자 지침

내 노트북에서 앱을 개발한 뒤, 맥미니의 **내 부서 폴더**에 붙여넣으면 자동으로 앱허브(포털)에
반영됩니다. **이 문서 하나면 됩니다.** 신규 앱을 만들든, 기존 앱(Supabase·SQLite 등)을 옮기든
여기 다 있습니다.

> 최종 업데이트: 2026-07-07 (변경 이력은 맨 아래)

---

## 0. 이 문서 쓰는 두 가지 방법

- **초심자(사람)**: 위에서부터 순서대로 따라 하세요. 명령은 그대로 복사-붙여넣기 하면 됩니다.
- **AI에게 맡기기(권장·가장 빠름)**: 당신의 AI(Claude Code, Cursor 등)에게
  **이 파일 전체를 붙여넣고** 아래처럼 지시하세요:
  > "이 지침에 맞춰 내 앱을 앱허브에서 바로 작동하도록 만들어/고쳐줘. 먼저 내 앱이 지금 DB를
  > 어떻게 쓰는지 파악하고, 지침의 해당 케이스대로 코드를 재구성해줘."

  그러면 AI가 당신 코드를 읽고 → 현재 DB 방식을 감지하고 → 이 지침의 규칙대로 바꿔줍니다.
  **당신은 이 앱이 지금 어떤 상태인지 몰라도 됩니다. AI가 파악합니다.**

> ⚠️ **가장 중요한 전제**: "내 앱은 이미 공유 DB로 돼 있겠지" 라고 가정하지 마세요.
> **아무것도 안 돼 있다고 가정하고** 이 지침대로 처음부터 맞추세요. 이미 돼 있으면 AI가 알아서 넘어갑니다.

---

# Part 1. 모든 앱 공통 기본

## 1. 한눈에 보는 흐름

1. 내 노트북에서 앱 개발 (템플릿 `AJW_APPHUB/templates/boilerplate/` 복제 추천)
2. 완성한 앱 폴더를 **`/Users/ajworld/Apps/dev/<부서>/<내이름>/<앱>/`** 에 붙여넣기(덮어쓰기)
3. **끝.** 약 1분 내 자동 감지 → 빌드 → 등록 → 기동 → 포털 대시보드에 카드로 나타남

수동 명령 필요 없음. (지금 바로 반영하고 싶으면 업로드 후 `touch .deploy` — 선택 사항)

## 🏗️ 앱허브는 이렇게 작동한다 (개발 전 필독)

내 앱이 **어디서·어떻게 도는지** 알아야 올바르게 만들 수 있습니다. 노트북에서만 되던 방식이
서버에선 안 되는 경우가 많으니 아래 7가지 환경을 이해하고 개발하세요.

**1) 어디서 도나 — 공용 서버의 도커 컨테이너**
- 앱은 내 노트북이 아니라 **회사 공용 서버(맥미니)의 도커 컨테이너**에서 돕니다(앱마다 컨테이너 1개,
  이름 `ajw-svc-<앱>`). 기본 자원은 **RAM 512MB / CPU 0.5** — 무거운 앱은 관리자와 상의.
- 그래서 로컬 절대경로, 특정 OS 도구, 하드코딩 포트 같은 "내 노트북 전제"는 서버에서 깨집니다.

**2) 어떻게 배포되나 — 업로드 = 이미지 통째 재빌드**
- 부서 폴더에 올리면 감시 데몬이 (업로드가 멈춘 걸 2회 확인 후) 감지 → `Dockerfile` 로 **이미지를
  새로 빌드** → 기존 컨테이너 교체 → `/health` 200 이면 라이브.
- ⚠️ **컨테이너 파일시스템은 매 배포마다 새로 만들어집니다(휘발성).** 앱이 실행 중 만든 파일(업로드물·
  sqlite·로그)은 **재배포하면 사라집니다.**

**3) 상태는 어디에 저장하나 — 공유 DB + (필요시) 볼륨**
- 업무 데이터 → **공유 postgres**(`DATABASE_URL`, Part 2). 전역변수/메모리에 담아두면 재시작 시 날아감.
- **사용자 업로드 파일**처럼 파일 영속이 필요하면 → `service.yml` 에 **`named_volume: true`** 를 넣으세요.
  그러면 컨테이너의 **`/data`** 경로가 영구 볼륨으로 유지됩니다. 파일은 반드시 `/data` 아래에 저장.
  (이 설정이 없거나 다른 경로면 재배포 시 사라짐.)

**4) 어떻게 접속되나 — 리버스 프록시 + 사내망 전용**
- 사용자는 **포털(`http://10.10.120.21:9000`) 대시보드의 카드**를 눌러 앱에 들어옵니다. 요청은
  **리버스 프록시(Traefik)** 가 각 앱 컨테이너의 **포트 3000** 으로 전달합니다 → **반드시 3000 listen**.
- 앱은 **사내망(LAN)에서만** 열립니다(외부 인터넷 노출 없음). 공유 DB도 사내망 전용.

**5) 로그인은 어떻게 — 포털 OAuth(SSO) 뒤에**
- 모든 앱은 **회사 포털 로그인 뒤**에 있습니다. 미로그인 사용자는 포털로 리다이렉트 →
  `/auth/callback` 에서 토큰 교환 → 세션. **앱이 자체 회원가입/비밀번호를 만들 필요 없습니다.**
  사용자 신원(이메일·이름·권한)은 포털이 줍니다. (템플릿이 이미 구현.)

**6) 설정은 어떻게 받나 — 환경변수 자동 주입**
- 앱허브가 컨테이너에 주입하는 값: `PORT`(=3000), `PORTAL_PUBLIC_URL`, `PORTAL_INTERNAL_URL`,
  `APP_PUBLIC_URL`, `OAUTH_CLIENT_ID`, `OAUTH_CLIENT_SECRET`, `SESSION_SECRET`, `DATABASE_URL`.
  **코드에 하드코딩하지 말고 `process.env.*` 로 읽으세요.**

**7) 공개 단계 — dev_live → internal_live**
- 처음 올리면 **나(owner)+관리자만** 보이는 `dev_live`. 검증 후 관리자가 **전 직원 공개(internal_live)**.
- 이미 전직원 공개된 앱을 수정하면, 무중단을 위해 **관리자 승인 후** 교체됩니다.

> **한 줄 요약**: 포트 **3000** / **`/health` 200** / 상태는 **공유 DB**(파일은 `/data` 볼륨) /
> 설정은 **env** / 로그인은 **포털 SSO** / **사내망 전용**. 이 6개를 지키면 노트북에서 만든 앱이
> 앱허브에서 그대로 돕니다.

## 2. 폴더 구조 — 어디에 올리나

**반드시 3단계**: `dev/<부서슬러그>/<내이름>/<서비스명>/`

| 부서 슬러그(폴더명) | 부서 | 예시 경로 |
|---|---|---|
| `sales` | 영업본부 | `dev/sales/홍길동/my-app/` |
| `marketing` | 마케팅팀 | `dev/marketing/홍길동/my-app/` |
| `qm` | 품질기술팀 | `dev/qm/홍길동/my-app/` |
| `general-affairs` | 총무팀 | `dev/general-affairs/홍길동/my-app/` |
| `scm` | SCM팀 | (특수 — Part 4 §17 참고) |

- **부서 폴더가 대시보드 분류(category)를 결정**합니다. `qm` 아래면 "품질기술팀"으로 묶임.
- 내 이름 폴더가 없으면 만들면 됩니다. 앱은 그 아래에.
- **폴더명(=앱 이름)** 은 소문자 영문/숫자/하이픈 권장(`my-app`). 대문자·공백 피하기.

## 3. 앱 폴더가 지켜야 할 계약

자동 빌드·기동이 되려면 아래를 지켜야 합니다:

1. **`Dockerfile`** 로 컨테이너 빌드 가능. (필수)
2. **포트 3000 에서 listen.** 앱허브는 컨테이너의 3000 포트로 연결합니다.
   - Node: `const PORT = process.env.PORT || 3000`. nginx 등: **반드시 `listen 3000;`** (80 아님!)
3. **`/health`** 경로가 HTTP 200 반환. (헬스체크 통과해야 라이브됨)
4. **포털 SSO** — 미로그인 시 포털로 리다이렉트, `/auth/callback` 에서 토큰 교환.
   자동 주입되는 환경변수: `PORT`, `PORTAL_PUBLIC_URL`, `PORTAL_INTERNAL_URL`, `APP_PUBLIC_URL`,
   `OAUTH_CLIENT_ID`, `OAUTH_CLIENT_SECRET`, `SESSION_SECRET` (템플릿이 이미 처리함).
5. **`.owner`** 파일에 **내 실제 포털 이메일**(예: `ai@ajw.co.kr`). 플레이스홀더 금지.
6. **`service.yml`** 에 `title`·`description` 채우기 → 대시보드 카드에 표시됨.

`Dockerfile` 의 `COPY` 경로는 **앱 폴더 기준**입니다(접두사 불필요).

## 4. 템플릿 사용 (권장)

`/Users/ajworld/Apps/AJW_APPHUB/templates/boilerplate/` 를 통째로 복제해 시작하세요.
`Dockerfile`·`server.mjs`(SSO+/health)·`service.yml`·`.owner` 가 다 들어 있습니다.

---

# Part 2. 데이터베이스는 이렇게 (가장 중요)

## 5. 대원칙 — 딱 3가지만 기억

1. **DB 는 사내 공유 postgres 서버(`ajw-apps-db`) 하나만** 씁니다.
   Supabase·Firebase 같은 외부 서비스, `*.db`(SQLite) 같은 로컬 파일 DB **모두 금지**(사내망 폐쇄 정책).
2. **접속은 오직 환경변수 `DATABASE_URL` 로만.** 코드에 주소·비밀번호 하드코딩 금지.
   ```js
   const url = process.env.DATABASE_URL;   // 이 한 줄로 접속. 주소는 몰라도 됨.
   ```
3. **스키마(테이블)는 앱이 부팅할 때 `CREATE TABLE IF NOT EXISTS` 로 스스로 만든다.**
   (운영 서버에서 사람이 수작업 SQL 안 치도록.)

> **자동 프로비저닝**: 앱을 처음 업로드하면 앱허브가 **이 앱 전용 DB + 계정을 자동 생성**하고,
> 접속정보를 내 앱 폴더에 **`.db-credentials`** 파일로 남깁니다. 운영 컨테이너에는 같은 DB를
> 가리키는 `DATABASE_URL` 이 자동 주입됩니다. **개발자가 서버에서 할 일은 없습니다.**

## 6. 신규 앱: 처음부터 이렇게 만들어라

새 앱이면 처음부터 아래대로 하면 이전 작업이 아예 필요 없습니다.

### 6-A. Node(Express/Next.js 등) — `pg` 사용

```bash
npm install pg
npm install -D @types/pg      # TypeScript 면
```

DB 모듈 하나 만들기 (`lib/db.js` 또는 `lib/db.ts`):

```js
// lib/db.js  — 공유 postgres 접속 + 부팅 시 스키마 자동생성
const { Pool } = require("pg");
if (!process.env.DATABASE_URL) throw new Error("DATABASE_URL 미설정 (.db-credentials 참고)");

const pool = new Pool({ connectionString: process.env.DATABASE_URL, max: 5 });

let ready = null;
function ensureDb() { if (!ready) ready = initSchema(); return ready; }   // 첫 쿼리 때 1회만

// 쓰기 편한 헬퍼: q=여러행, one=첫행, run=INSERT/UPDATE/DELETE
async function q(sql, params = [])   { await ensureDb(); return (await pool.query(sql, params)).rows; }
async function one(sql, params = []) { return (await q(sql, params))[0]; }
async function run(sql, params = []) { await ensureDb(); return pool.query(sql, params); }

async function initSchema() {
  await pool.query(`
    CREATE TABLE IF NOT EXISTS items (
      id     bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
      name   text NOT NULL,
      amount double precision DEFAULT 0,
      created_at text DEFAULT to_char(now() AT TIME ZONE 'Asia/Seoul','YYYY-MM-DD HH24:MI:SS')
    );
  `);
  // 컬럼을 나중에 추가할 땐: ALTER TABLE items ADD COLUMN IF NOT EXISTS memo text DEFAULT '';
}

module.exports = { pool, q, one, run };
```

사용 (파라미터는 `$1, $2 ...` 순서, 값은 배열로):

```js
const { q, one, run } = require("./lib/db");
// 조회
const rows = await q("SELECT * FROM items WHERE amount > $1 ORDER BY id", [1000]);
const row  = await one("SELECT * FROM items WHERE id = $1", [id]);
// 생성 (새 id 필요하면 RETURNING)
const r = await run("INSERT INTO items (name, amount) VALUES ($1,$2) RETURNING id", ["볼트", 500]);
const newId = r.rows[0].id;
// 수정/삭제
await run("UPDATE items SET amount = $1 WHERE id = $2", [700, id]);
await run("DELETE FROM items WHERE id = $1", [id]);
```

> **중요**: `pg` 는 **비동기**입니다. 모든 DB 호출에 `await` 를 붙이고, 그 함수는 `async` 여야 합니다.
> Next.js면 API 라우트(`app/api/.../route.ts`)는 이미 async, 서버 컴포넌트는
> `export default async function Page()` 로 만들면 본문에서 `await` 가능합니다.

### 6-B. Python(FastAPI/Flask 등) — SQLAlchemy 사용

```bash
pip install "sqlalchemy>=2" "psycopg[binary]"   # requirements.txt 에도 추가
```

```python
import os
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, DeclarativeBase

url = os.getenv("DATABASE_URL", "postgresql+psycopg://localhost/dev")
if url.startswith("postgresql://"):                 # .db-credentials 값 보정
    url = url.replace("postgresql://", "postgresql+psycopg://", 1)
connect_args = {"check_same_thread": False} if url.startswith("sqlite") else {}

engine = create_engine(url, connect_args=connect_args, pool_pre_ping=True)
SessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False)

class Base(DeclarativeBase): pass
# 모델 정의 후, 앱 시작 시: Base.metadata.create_all(engine)  ← 스키마 자동생성
```

### 6-C. 로컬에서 개발·테스트하는 법

공유 DB 접속정보(`.db-credentials`)는 **앱을 처음 업로드한 뒤** 앱 폴더에 생깁니다.
그 전이나, 안전하게 실험만 하고 싶을 땐 **내 노트북에 임시 postgres** 를 띄워 쓰세요:

```bash
# 노트북에 임시 DB (도커 필요)
docker run -d --name devpg -e POSTGRES_PASSWORD=dev -p 5432:5432 postgres:15
export DATABASE_URL="postgresql://postgres:dev@localhost:5432/postgres"
# 이제 앱 실행 → 위 initSchema 가 테이블 만들고 동작
```

업로드 후 앱 폴더에 생긴 `.db-credentials` 의 `DATABASE_URL`(사내망 LAN `10.10.120.21:5433`)을
쓰면 **운영과 같은 DB** 로 개발됩니다(사내망에서만 접속). 단 그 경우는 **운영 데이터에 직접 쓰는 것**이니
Part 3 §13 의 주의사항을 지키세요.

### 6-D. 절대 하지 말 것

- ❌ `better-sqlite3`, `sqlite3`, `*.db` 파일 — 로컬 파일 DB 금지.
- ❌ `@supabase/supabase-js` 등 외부 클라우드 DB 금지.
- ❌ `DATABASE_URL`·`.db-credentials` 를 git 커밋. → `.gitignore` 에 넣기.
- ❌ 접속정보 코드 하드코딩.

## 7. `.db-credentials` 흐름 정리

```
[앱 업로드] → 앱허브가 감지 → app_<앱이름> DB + 전용계정 자동생성
           → 운영 컨테이너에 DATABASE_URL 자동주입
           → 내 앱 폴더에 .db-credentials 기록(개발자 로컬용 LAN URL)
```
- `.db-credentials` 형식:
  ```
  DATABASE_URL=postgresql://<user>:<pw>@10.10.120.21:5433/app_<앱>
  DB_HOST=10.10.120.21
  DB_PORT=5433
  DB_NAME=app_<앱>
  DB_USER=<user>
  DB_PASSWORD=<pw>
  ```
- **비밀입니다. 커밋·업로드 금지.** 이 계정은 자기 앱 DB에만 접근 가능.
- 파일이 안 생겼거나 필요하면 **관리자에게 "○○ 앱 공유 DB 프로비저닝 + `.db-credentials` 요청"**.

---

# Part 3. 기존 앱을 공유 DB로 이전 (Supabase·SQLite 등 쓰던 경우)

> 각 앱 폴더에는 그 앱에 맞춘 **`공통db마이그레이션.md`** 도 있습니다(자기 앱이 어떤 케이스인지
> 배너로 표시). 이 Part 3 는 그 내용을 모두 담은 **공통 완전판**입니다. 아래 절대 규칙을 먼저 읽으세요.

## 절대 규칙 (어기면 운영사고)

1. **공유 postgres = 운영 DB 그 자체.** dev·운영이 **같은 DB**를 봅니다(완전 공유 방침).
   따라서 노트북에서 공유 DB에 붙여 앱을 돌리면 **운영 데이터에 직접 쓰기**가 됩니다.
2. **로컬의 기존 데이터를 공유 DB에 마음대로 대량 INSERT 하지 마라.** 오래됐거나 테스트 데이터일 수
   있어 운영을 오염시킵니다. 개발자(AI)는 **코드·스키마·쿼리 변환 + 동작 검증**까지만.
   **실제 운영 데이터 이관은 §14 처럼 관리자와 함께 1회** 수행합니다.
3. **기존 로컬 DB(`*.db`, `prisma/*.db`)·Supabase 프로젝트를 삭제하지 마라.** 이관 원본·롤백용.
4. 파괴적 명령 금지: `DROP DATABASE`, `TRUNCATE`, `prisma migrate reset`, `rm -rf`.

## 8. 먼저: 내 앱이 어떤 케이스인지 감지

앱 루트에서:

```bash
echo "== better-sqlite3? =="; grep -rl "better-sqlite3" . 2>/dev/null | grep -v node_modules | head
echo "== prisma? ==";         find . -name schema.prisma -not -path '*/node_modules/*'
echo "== supabase? ==";       grep -rl "supabase" . 2>/dev/null | grep -vE "node_modules|package-lock" | head
echo "== python/ORM? ==";     grep -rl "sqlalchemy\|psycopg\|sqlite3" . 2>/dev/null | grep -v node_modules | head
echo "== 기존 DB 파일 ==";     find . \( -name '*.db' -o -name '*.sqlite' \) -not -path '*/node_modules/*'
```

- `better-sqlite3`(Node, 동기) → **케이스 A** (§9)
- `schema.prisma` + `provider="sqlite"` → **케이스 B** (§10)
- `supabase` 를 **브라우저에서 직접 호출**(Vite/React·순수 HTML 등) → **케이스 C** (§11)
- **Python** + SQLAlchemy/`sqlite3` → **케이스 D** (§12)
- 아무 DB 흔적 없음 → 이전할 것 없음. (파일·외부API 로 저장하는 게 있는지만 개발자에게 확인)

## 9. 케이스 A — better-sqlite3(동기) → pg(비동기)

better-sqlite3 는 동기, pg 는 비동기. **모든 DB 호출에 `await`, 함수는 `async`.**

1. **설치**: `npm install pg` (`better-sqlite3` 는 데이터 이관용으로 당분간 **남겨둠**).
2. **DB 모듈 교체**: §6-A 의 `pool`+`q/one/run`+`initSchema` 로 바꾼다. 기존 `getDb()` 는 제거.
3. **호출부 1:1 치환**:

   | better-sqlite3 (동기) | pg (비동기) |
   |---|---|
   | `db.prepare(sql).get(a,b)` | `await one(sql, [a,b])` |
   | `db.prepare(sql).all(a)` | `await q(sql, [a])` |
   | `db.prepare(sql).run(a,b)` | `await run(sql, [a,b])` |
   | `stmt.run(...).lastInsertRowid` | `(await run(sql+" RETURNING id", [...])).rows[0].id` |
   | `db.exec("...")` | `await run("...")` (또는 initSchema 에) |
   | `db.transaction(fn)` | 아래 6번 트랜잭션 |

   - **플레이스홀더**: SQLite `?` → postgres `$1,$2,...`(1부터 순서대로).
   - 동적 컬럼 조립 예:
     ```js
     const set = fields.map((f,i)=>`${f} = $${i+1}`).join(", ");
     await run(`UPDATE t SET ${set} WHERE id = $${fields.length+1}`, [...values, id]);
     ```
4. **스키마 DDL 변환표** (initSchema 에 넣을 때):

   | SQLite | postgres |
   |---|---|
   | `INTEGER PRIMARY KEY AUTOINCREMENT` | `bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY` |
   | `REAL` | `double precision` |
   | `INTEGER`(0/1 불리언 포함) | `integer` (0/1 그대로 유지 권장) |
   | `TEXT` | `text` |
   | `TEXT DEFAULT (datetime('now','localtime'))` | `text DEFAULT to_char(now() AT TIME ZONE 'Asia/Seoul','YYYY-MM-DD HH24:MI:SS')` |
   | `... REFERENCES t(id) ON DELETE CASCADE`, `UNIQUE(a,b)`, `CREATE INDEX IF NOT EXISTS` | 동일 |
   | `PRAGMA journal_mode`, `PRAGMA foreign_keys` | **삭제** |

5. **인라인 SQL 함수/구문 변환**:
   - `datetime('now','localtime')` → `to_char(now() AT TIME ZONE 'Asia/Seoul','YYYY-MM-DD HH24:MI:SS')`
   - `INSERT OR IGNORE` → `INSERT ... ON CONFLICT DO NOTHING`
   - `INSERT OR REPLACE` → `INSERT ... ON CONFLICT(<키>) DO UPDATE SET ...`
   - `IFNULL(a,b)` → `COALESCE(a,b)`. `||`(문자열연결) 동일. `LIMIT/OFFSET` 동일.
   - `PRAGMA table_info` 로 컬럼 추가하던 로직 → `ALTER TABLE t ADD COLUMN IF NOT EXISTS col type DEFAULT ...` 한 줄로.
6. **트랜잭션**(있으면):
   ```js
   async function withTx(fn){ const c=await pool.connect();
     try{ await c.query("BEGIN"); const r=await fn(c); await c.query("COMMIT"); return r; }
     catch(e){ await c.query("ROLLBACK"); throw e; } finally{ c.release(); } }
   ```

## 10. 케이스 B — Prisma(sqlite) → Prisma(postgresql)

1. `schema.prisma` datasource:
   ```prisma
   datasource db { provider = "postgresql"  // sqlite → postgresql
                   url = env("DATABASE_URL") }
   ```
2. `@prisma/adapter-better-sqlite3`·`better-sqlite3` 어댑터 설정 제거, 기본 `new PrismaClient()` 로.
3. `npm install pg` → `npx prisma generate`.
4. 스키마 적용(데이터 아님): 빈 공유 DB면 `npx prisma migrate deploy`.
   히스토리가 sqlite 기준이라 꼬이면 baseline 새로 생성. **`prisma migrate reset` 절대 금지.**
5. 모델 타입은 대부분 호환(`Int @id @default(autoincrement())`, `DateTime`, `Float`, `String`, `Boolean`).
6. 데이터 이관은 여기서 안 함(§14 관리자).

## 11. 케이스 C — Supabase(브라우저 직접호출) → 사내 postgres ⚠️ 큰 작업

**브라우저는 사내 postgres에 직접 접속 못 합니다**(보안·네트워크·비밀노출). 그래서 단순 교체가 아니라
**백엔드 API 신설**이 필요합니다. (순수 HTML 앱이면 백엔드가 아예 없으니 서버부터 만들어야 함.)

### 11-1. 코드 재구성
- **얇은 백엔드 추가**(Express/Fastify 등): 서버가 `DATABASE_URL` 로 pg 접속(§6-A 패턴).
  프론트의 `supabase.from('t').select()/insert()/update()/delete()` 호출들을 각각
  `fetch('/api/...')` 로 바꾸고 서버에 해당 엔드포인트 구현. **또는** Next.js 로 재구성(route handler).
- Supabase **Auth** → 회사 포털 **OAuth(SSO)** 로 대체(§3-4, 템플릿 참고).
- Supabase **Storage/Realtime** 쓰면 → 컨테이너 볼륨 / 폴링·SSE 등으로 대체.
- 한 번에 다 바꾸지 말고 **백엔드 골격 + 1개 엔드포인트** 부터 검증 후 확장.

### 11-2. ⭐ 기존 Supabase 데이터 → 공유 pg 이관 (필수)

Supabase 자체가 관리형 postgres라 **postgres→postgres 복사**로 깔끔합니다.
**관리자와 함께 1회** 수행(절대 규칙 2). 로컬에 `postgresql-client`(pg_dump/pg_restore/psql) 필요.

**준비물**: Supabase 접속 문자열 — 대시보드 → **Project Settings → Database → Connection string(URI)**.
형식: `postgresql://postgres:<PW>@db.<ref>.supabase.co:5432/postgres`.

```bash
# 1) Supabase에서 앱 테이블만 덤프 (내부 스키마 제외)
SUPA="postgresql://postgres:<PW>@db.<ref>.supabase.co:5432/postgres"
pg_dump "$SUPA" --schema=public --no-owner --no-privileges --no-comments -Fc -f supa.dump
#   특정 테이블만: -t public.<테이블> 을 여러 번

# 2) 공유 pg(app_<앱> DB)로 복원  (DBURL = .db-credentials 의 DATABASE_URL)
DBURL="$(grep '^DATABASE_URL=' .db-credentials | cut -d= -f2-)"
pg_restore --no-owner --no-privileges --no-acl -d "$DBURL" supa.dump

# 3) 검증: 양쪽 행 수 비교
psql "$SUPA"  -tAc "SELECT count(*) FROM public.<테이블>"
psql "$DBURL" -tAc "SELECT count(*) FROM public.<테이블>"
```

**주의**:
- `auth`·`storage`·`realtime`·`vault` 등 **Supabase 내부 스키마는 가져오지 않음**(앱 테이블만).
- **RLS 정책·GRANT** 는 공유 pg에서 불필요(백엔드가 접근제어). 복원 중 관련 에러는 무시 가능.
- Supabase 확장(`uuid-ossp`, `pgcrypto` 등)이 필요하면 앱 계정 권한이 없을 수 있음 →
  **관리자에게 "CREATE EXTENSION 요청"**.
- **딱 1회**, **비어있는 app DB**(검증용 테스트행 먼저 삭제)에 복원. 시퀀스 값은 덤프에 포함돼 이어짐.
- 공유 pg로 안정 동작 확인 전까지 Supabase 프로젝트는 **롤백용으로 유지**.

## 12. 케이스 D — Python(SQLAlchemy/sqlite3) → 사내 postgres (보통 가장 쉬움)

1. `pip install "psycopg[binary]"` (requirements.txt 추가).
2. 접속 URL: §6-B 처럼 `DATABASE_URL` 을 `postgresql+psycopg://` 로 보정하고, sqlite 전용
   `connect_args={"check_same_thread": False}` 는 postgres에서 분기 제거.
3. 스키마: `Base.metadata.create_all(engine)` (또는 `alembic upgrade head`) → postgres에도 자동 생성.
4. 원시 SQL 이 있으면 §9-5 처럼 sqlite 함수/구문을 postgres로. `sqlite3` 표준라이브러리 직접 사용 시
   `psycopg` 로 교체(플레이스홀더 `?` → `%s`).

## 13. 로컬 검증 절차 (모든 케이스)

```bash
npm install                              # 또는 pip install -r requirements.txt
export DATABASE_URL="$(grep '^DATABASE_URL=' .db-credentials | cut -d= -f2-)"
# 앱 실행 (npm run dev / npm start / uvicorn ... 등)
```
확인:
- [ ] 에러 없이 뜨고 스키마가 공유 DB에 생성됨.
- [ ] 로그인·주요 화면·`/health`(200) OK.
- [ ] 데이터 **생성→조회→수정→삭제** 가 실제 반영됨.
- [ ] 비동기 전환 누락으로 `[object Promise]` 가 화면에 뜨는 버그 없음.
- [ ] 검증용 테스트 레코드는 **지우고** 마무리(운영 오염 방지).

그리고 마무리:
1. `.gitignore` 에 `.db-credentials`, `.env*`, `*.db` 확인.
2. 앱 폴더를 부서 폴더에 업로드(덮어쓰기) → 약 1분 내 자동 재빌드.
3. 관리자에게: **"○○ 앱 공유 DB 전환 완료 — 운영 실데이터 이관 요청"**.

## 14. (관리자용) 운영 실데이터 1회 이관

개발자 AI는 실행하지 말 것. 관리자가 서버에서(또는 개발자 입회 하) 1회 수행.
- **(a) sqlite/파일 DB(케이스 A·D)**: 운영 볼륨의 `*.db` 를 원본으로 공유 pg에 **누락분만 백필 +
  완료표식**(멱등). 관계형은 부모→자식 순서 INSERT 후 시퀀스 리셋
  (`SELECT setval(pg_get_serial_sequence('t','id'),(SELECT COALESCE(MAX(id),1) FROM t))`).
  이미 pg에서 운영 중이었다면 **덮어쓰지 말고 무손실 병합**. (order-alarm 선례.)
- **(b) Supabase(케이스 C)**: §11-2 의 `pg_dump`→`pg_restore`. 비어있는 DB에 1회, 행 수 검증.

---

# Part 4. 운영

## 15. 기존 앱 수정
- 앱 폴더 코드를 고쳐 **같은 위치에 덮어쓰기** → 약 1분 내 자동 재빌드·반영.
- **폴더명(=앱 이름) 변경 금지**(바꾸면 다른 앱으로 취급).

## 16. 공개 범위
- 올리면 처음엔 **나(owner)+관리자만** 보임(dev_live).
- 테스트 후 **관리자에게 "전 직원 공개" 요청** → internal_live 로 전환.

## 17. 부서별 특이사항
### SCM팀 (`dev/scm/<내이름>/<앱>`)
SCM은 3개 앱(**AJW 생산자재 발주계획 / OJC 발주 / OJC ORDER**)이 하나의 게이트웨이로 서빙되는
**특수 구조**입니다. 자기 앱 폴더를 `dev/scm/<내이름>/<앱이름>` 에 덮어쓰면 게이트웨이가 재빌드됩니다.
앱 폴더명은 위 3개 중 하나와 정확히 일치해야 함(변경 금지). 새 SCM 앱 추가는 관리자와 상의.

## 18. 반영 결과 확인
- 온보딩 로그: `AJW_APPHUB/logs/onboard/<앱>.log`
- 기존앱 자동동기화 로그: `AJW_APPHUB/logs/sales-autosync/autosync.log`
- 안 뜨면 위 로그에서 빌드/헬스 오류 확인 + §19 체크.

## 19. 자주 하는 실수
- ❌ `.owner` 플레이스홀더(`your-email@ajw.co.kr`) → 접근 불가. **내 실제 이메일**로.
- ❌ 80 포트 listen → 화면 안 뜸. **3000 으로.**
- ❌ `/health` 없음/200 아님 → 라이브 안 됨.
- ❌ 2단계 경로(`dev/<내이름>/<앱>`) → **부서 3단계**(`dev/<부서>/<내이름>/<앱>`)로.
- ❌ 로컬 파일 DB(sqlite)/Supabase 사용 → **공유 pg + `DATABASE_URL`**(Part 2).
- ❌ `.db-credentials`/`.env`/`*.db` 커밋·업로드.

---

## 변경 이력

- **2026-07-07**: dev 폴더를 **부서-우선 구조**(`dev/<부서>/<내이름>/<앱>`)로 전환.
  수동 `.deploy` 선택 사항화. 카드 제목은 `service.yml`. 포트 3000 + `/health` 필수.
- **2026-07-07 (2)**: DB를 **사내 공유 postgres**(`ajw-apps-db`)로 통일. Supabase·로컬 파일 DB 폐지.
  `DATABASE_URL` 접속, 온보딩이 앱별 DB·계정 자동생성 + `.db-credentials` 전달.
- **2026-07-07 (3)**: 앱별 **`공통db마이그레이션.md`** 지침서 도입(order-alarm 이전 완료 선례).
- **2026-07-07 (4)**: 본 지침을 완전판으로 재작성 — 신규 앱 DB 구성법(Node `pg`/Python
  SQLAlchemy, 스키마 자동생성, 로컬 임시 postgres)과 기존 앱 이전(케이스 A/B/C/D + Supabase
  데이터 `pg_dump`→`pg_restore` 이관)을 초심자·AI 모두 따라할 수 있게 단계별로 통합.
- **2026-07-07 (5)**: **"앱허브는 이렇게 작동한다(개발 전 필독)"** 섹션 추가 — 런타임 환경 7가지
  (공용서버 도커 컨테이너·재빌드/휘발성 파일시스템·상태는 공유DB+`/data` 볼륨(`named_volume`)·
  프록시가 포트3000 라우팅·사내망 전용·포털 OAuth·env 주입·dev_live/internal_live)를 설명해,
  개발자가 앱허브 환경에 맞춰 개발하도록 함.
